import React from 'react'
import style from '../../scss/Auth.module.scss'

export const AuthComponent = ({isAuth, ChangeIsAuth}: {isAuth: boolean, ChangeIsAuth: {}}) => {
  return (
    <div className={style.auth}>
        auth
    </div>
  )
}
